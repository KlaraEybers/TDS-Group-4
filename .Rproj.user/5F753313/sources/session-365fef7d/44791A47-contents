rm(list = ls())

# Import packages ---------------------------------------------------------

library(here)
library(dplyr)
library(FactoMineR)
library(factoextra)

# Import data -------------------------------------------------------------

df <- readRDS(here("00_data", "01_processed", "ukb_train_imputed.rds"))

# Remove ID and outcome variables ----------------------------------------

df_analysis <- df %>%
  select(-eid, -cvd_outcome)   # change names if different

colnames(df)